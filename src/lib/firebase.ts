// Placeholder to maintain compatibility; Firebase has been removed.
// Firebase deprecated; Supabase is now used. Keeping file to avoid import errors during transition.
export const auth = null as any;
export const db = null as any;
