/**
 * Inert test substitute for the `server-only` module. Vitest aliases
 * `server-only` to this file so server-only modules can be exercised in the
 * node test runtime without the bundler-level client guard.
 */
export {};
